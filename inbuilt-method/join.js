// whenever we want to convert string into array wer use join()
// it won't modify original array
// (,) default

let fruits = ["orange", "apple", "kiwi", "coconait"];

// let str = fruits.join(" ");
let str = fruits.join(" and ");
console.log(str);
// console.log(fruits)



let number = [1, 2, 3, 4, 5, 6, 6];
let numStr = number.join("");
console.log(numStr);
